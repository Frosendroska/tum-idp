boto3>=1.34.0
botocore[crt]>=1.34.0  # Required: botocore with CRT support for high-performance transfers
aioboto3>=12.0.0  # Async boto3 for high-performance operations
awscrt==0.19.17  # Must match botocore[crt] constraint to avoid resolver conflicts
uvloop>=0.19.0  # Required: Faster event loop implementation (2-4x faster than default asyncio)
aiohttp>=3.9.0  # Required: Exception handling for async HTTP operations
pandas>=2.0.0
fastparquet>=2023.10.1
matplotlib>=3.7.0
numpy>=1.24.0
pyarrow>=10.0.0  # Required: Backend for parquet file operations
